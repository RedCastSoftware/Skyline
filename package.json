{
  "name": "puppeteer-proxy",
  "version": "1.0.0",
  "description": "A simple proxy server using Puppeteer to render pages",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^20.9.0"
  }
}
